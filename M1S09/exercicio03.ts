let aluno: {
  nome: string;
  matricula: number;
  ativo: boolean;
  cadeiras: string[];
} = {
  nome: 'vitor',
  matricula: 1244,
  ativo: true,
  cadeiras: ['mtm', 'fsc', 'bio', 'qmc'],
};

console.log(aluno);
